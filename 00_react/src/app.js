
const App = () => {
      return (
        <div>
          <h1>CoreFrame - React Scratch</h1>
          <p>React 18 setup from scratch using CDN.</p>
        </div>
      );
    };

    const root = ReactDOM.createRoot(document.getElementById("root"));
    root.render(<App/>);